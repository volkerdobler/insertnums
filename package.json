{
	"name": "insertnums",
	"version": "1.1.2",
	"publisher": "volkerdobler",
	"displayName": "Insert Sequences",
	"description": "Insert sequences of any kind (numberic, alpha, expressions, etc.) to VSCode textdocuments",
	"keywords": [
		"sequences",
		"numbers",
		"insert",
		"alphachars",
		"expressions",
		"frequencies",
		"insertnums",
		"insertseq",
		"insertsequences"
	],
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/volkerdobler/insertseq/issues",
		"email": "vscode.insertsequences.ext@volker-dobler.de"
	},
	"homepage": "https://github.com/volkerdobler/insertseq/blob/master/README.md",
	"repository": {
		"type": "git",
		"url": "https://github.com/volkerdobler/insertseq.git"
	},
	"engines": {
		"vscode": "^1.120.0"
	},
	"categories": [
		"Other"
	],
	"main": "./dist/extension.js",
	"browser": "./dist/extension.js",
	"files": [
		"dist",
		"README.md",
		"CHANGELOG.md",
		"LICENSE.md",
		"EXAMPLES.md",
		"WHATSNEW*.md",
		"package.json",
		"insertseq.png"
	],
	"contributes": {
		"configuration": {
			"title": "insertseq",
			"properties": {
				"insertseq.start": {
					"type": "string",
					"default": "1",
					"description": "Default start"
				},
				"insertseq.step": {
					"type": "string",
					"default": "1",
					"description": "Default increment"
				},
				"insertseq.repetition": {
					"type": "string",
					"default": "",
					"description": "Default repetition"
				},
				"insertseq.frequency": {
					"type": "string",
					"default": "1",
					"description": "Default frequency"
				},
				"insertseq.startover": {
					"type": "string",
					"default": "",
					"description": "Start sequence from beginning, independent of repetition and frequency"
				},
				"insertseq.numberFormat": {
					"type": "string",
					"default": "",
					"description": "Default output format for numbers"
				},
				"insertseq.stringFormat": {
					"type": "string",
					"default": "",
					"description": "Default output format for strings"
				},
				"insertseq.dateFormat": {
					"type": "string",
					"default": "",
					"description": "Default output format for dates"
				},
				"insertseq.alphaCapital": {
					"type": "string",
					"default": "preserve",
					"enum": [
						"preserve",
						"upper",
						"lower",
						"pascal"
					],
					"enumDescriptions": [
						"preserve (default): preserve chars as insert",
						"upper: output all chars as upper letters",
						"lower: output all chars as lower letters",
						"pascal: output first letter as upper letter, rest as lower letters"
					],
					"description": "Default output in alpha sequences"
				},
				"insertseq.language": {
					"type": "string",
					"default": "",
					"description": "Default language format for dates"
				},
				"insertseq.insertOrder": {
					"type": "string",
					"default": "cursor",
					"deprecationMessage": "Option is deprecated: use new Options 'sortedOutput' and 'reversedOutput'",
					"description": "Default order of insertion",
					"enum": [
						"cursor",
						"sorted",
						"reverse"
					],
					"enumDescriptions": [
						"cursor (default): insertion is based on multi cursor click order",
						"sorted: insertion is from top most click down",
						"reverse: insertion is from bottom most click up"
					]
				},
				"insertseq.sortedOutput": {
					"type": "boolean",
					"default": false,
					"description": "If true, insertion is from top most click down; if false (default), insertion is based on multi cursor click order"
				},
				"insertseq.reversedOutput": {
					"type": "boolean",
					"default": false,
					"description": "If true, insertion is is reversed; if false (default), insertion is done in 'normal' order"
				},
				"insertseq.century": {
					"type": "string",
					"default": "20",
					"description": "Default century if year has only 2 digits - default: 20"
				},
				"insertseq.centerString": {
					"type": "string",
					"default": "l",
					"enum": [
						"l",
						"r"
					],
					"enumDescriptions": [
						"l (default): e.g. a 1 char string centered in a 4 char field is left oriented (placed at position 2)",
						"r: e.g. a 1 char string centered in a 4 char field is right oriented (placed at position 3)"
					]
				},
				"insertseq.dateStepUnit": {
					"type": "string",
					"default": "d",
					"description": "Default unit as date step",
					"enum": [
						"d",
						"w",
						"m",
						"y"
					],
					"enumDescriptions": [
						"d (default): days",
						"w: weeks",
						"m: months",
						"y: years"
					]
				},
				"insertseq.delimiter": {
					"type": "string",
					"default": "",
					"description": "Delimiter, for additional insertations"
				},
				"insertseq.alphabet": {
					"type": "string",
					"default": "abcdefghijklmnopqrstuvwxyz",
					"description": "Alphabet, for string successors"
				},
				"insertseq.mysequences": {
					"type": "array",
					"default": [
						[
							"Jan",
							"Feb",
							"Mar",
							"Apr",
							"May",
							"Jun",
							"Jul",
							"Aug",
							"Sep",
							"Oct",
							"Nov",
							"Dec"
						],
						[
							"foo",
							"bar",
							"foz"
						]
					],
					"description": "Own defined Sequences (Array of arrays)"
				},
				"insertseq.myfunctions": {
					"type": "array",
					"default": [
						"(i, start=1, step=1) => start + (i ** step)",
						"(i) => Math.PI.toString().split('').splice(i,1)"
					],
					"description": "Own defined Functions (Array of functions) - function arguments are (i, start, step, frequency, repeat, startover), where i is the zero-based index of the insertion, start is the start value, step is the step value, frequency is the frequency value, repeat is the repetition value, and startover is the startover value (all beside i are optional, if you use them, give default values)"
				},
				"insertseq.defaultFunctionNr": {
					"type": "number",
					"default": 1,
					"description": "Default function number (1 = first function in 'myfunctions' list)"
				},
				"insertseq.radixPrefix": {
					"type": "boolean",
					"default": false,
					"description": "Whether to use radix prefixes (0b, 0o, 0x) when inserting numbers in binary, octal, or hexadecimal format"
				},
				"insertseq.previewColor": {
					"type": "string",
					"default": "#888888",
					"description": "Color of preview decoration"
				},
				"insertseq.previewStatus": {
					"type": "boolean",
					"default": true,
					"description": "Show preview of insertions"
				},
				"insertseq.maxInsertions": {
					"type": "number",
					"default": 10000,
					"description": "Maximum number of insertions to avoid performance issues - internal variable, be aware what you are doing if you change it"
				},
				"insertseq.maxHistoryItems": {
					"type": "number",
					"default": 100,
					"description": "Maximum items stored in history"
				},
				"insertseq.debug": {
					"type": "boolean",
					"default": false,
					"description": "Enable debug output"
				}
			}
		},
		"commands": [
			{
				"command": "extension.insertseq",
				"title": "Insert Sequences"
			},
			{
				"command": "extension.insertseq.history",
				"title": "Insert Sequences-History"
			}
		],
		"keybindings": [
			{
				"command": "extension.insertseq",
				"key": "ctrl+alt+.",
				"mac": "cmd+alt+.",
				"when": "editorTextFocus"
			},
			{
				"command": "extension.insertseq.history",
				"key": "ctrl+alt+,",
				"mac": "cmd+alt+,",
				"when": "editorTextFocus"
			}
		]
	},
	"scripts": {
		"check-types": "tsc --noEmit",
		"clean": "rimraf ./dist/* -g",
		"compile:desk": "tsc -p ./",
		"compile:web": "tsc -p ./",
		"compile:all": "npm run clean && npm run compile:desk",
		"build:desk": "npm run check-types && npm run lint && node esbuild.js --production",
		"build:web": "npm run check-types && npm run lint && node esbuild-web.js --production",
		"build:all": "npm run clean && npm run build:desk",
		"lint": "eslint src --ext .ts",
		"watch:desk": "tsc -watch -p ./",
		"watch:web": "tsc -watch -project tsconfig.json & node esbuild-web.js --watch",
		"watch:all": "npm run clean && npm-run-all --parallel watch:desk watch:web",
		"run-in-browser": "vscode-test-web --browserType=chromium ./dist/extension.js",
		"vscode:prepublish": "npm run build:all",
		"test": "npm run compile:desk && node dist/formatting.test.js"
	},
	"dependencies": {
		"d3-format": "^3.1.0",
		"eval5": "^1.4.8",
		"temporal-polyfill": "^0.3.0"
	},
	"devDependencies": {
		"@esbuild-plugins/node-globals-polyfill": "^0.2.3",
		"@types/d3-format": "^3.0.4",
		"@types/node": "^25.0.0",
		"@types/vscode": "^1.105.0",
		"@typescript-eslint/eslint-plugin": "^8.56.1",
		"@typescript-eslint/parser": "^8.56.1",
		"@vscode/test-web": "^0.0.80",
		"esbuild": "^0.28.0",
		"eslint": "^10.0.0",
		"eslint-plugin-prettier": "^5.5.4",
		"glob": "^13.0.0",
		"npm-run-all": "^4.1.5",
		"prettier": "^3.2.5",
		"typescript": "^6.0.3"
	},
	"icon": "insertseq.png",
	"galleryBanner": {
		"color": "#5757ff",
		"theme": "dark"
	}
}